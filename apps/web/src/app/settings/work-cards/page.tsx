import { SettingsPlaceholder } from "@/components/settings-placeholder";

export default function WorkCardsSettingsPage() {
  return (
    <SettingsPlaceholder
      title="Work cards"
      description="Configure which fields appear on work cards, defaults, and custom fields across the workspace."
    />
  );
}
